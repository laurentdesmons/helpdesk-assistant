[project]
name = "helpdesk-assistant"
version = "0.1.0"
description = "IT Help Desk Agent Assistant — LangGraph orchestrator + Foundry-hosted MAF agents"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.7",
    "pydantic-settings>=2.2",
    "python-dotenv>=1.0",
    "rich>=13.7",
    "filelock>=3.13",
    "opentelemetry-api>=1.24",
    "httpx>=0.27",
    # --- Phase 1: classifier (Foundry GPT model via FoundryChatClient) ---
    "agent-framework",
    "agent-framework-foundry",             # FoundryChatClient
    "azure-ai-projects>=2.2",
    "azure-identity>=1.17",
    # --- Phase 1 deploy: classifier as a Foundry hosted agent ---
    "agent-framework-foundry-hosting",     # pre-release — ResponsesHostServer
    # --- Phase 2: KB / Azure AI Search. Phase 3: also runtime for the resolver
    #     container (mirrored into requirements.txt). ---
    "azure-search-documents>=12.1.0b2",    # pre-release — vector + semantic hybrid
    "openai>=1.40",                        # embeddings via AIProjectClient.get_openai_client()
    # Added in later phases:
    #   Phase 4: langgraph, langchain-azure-ai[tools,opentelemetry],
    #            azure-monitor-opentelemetry-exporter (App Insights span export)
]

[project.scripts]
helpdesk-classifier-host = "helpdesk.agents.classifier.host:main"
helpdesk-resolver-host = "helpdesk.agents.resolver.host:main"

[project.optional-dependencies]
dev = [
    "pytest>=8.1",
    "pytest-asyncio>=0.23",
    "pytest-cov>=5.0",
    "ruff>=0.4",
    "mypy>=1.10",
]
# LLM-judge groundedness for the resolver eval (README §7). Off by default — the
# resolver eval is code-based; this is scaffolding until a real KB exists.
eval = [
    "azure-ai-evaluation>=1.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv]
# agent-framework-foundry-hosting is published as a pre-release.
prerelease = "allow"

[tool.hatch.build.targets.wheel]
packages = ["src/helpdesk"]

[tool.pytest.ini_options]
pythonpath = ["src"]
asyncio_mode = "auto"
testpaths = ["tests"]
addopts = "-q"

[tool.ruff]
line-length = 110
src = ["src", "tests", "eval", "scripts"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]

[tool.mypy]
python_version = "3.11"
packages = ["helpdesk"]
mypy_path = "src"
strict = true
warn_unused_ignores = true

[[tool.mypy.overrides]]
module = ["agent_framework_foundry_hosting.*"]
ignore_missing_imports = true

[[tool.mypy.overrides]]
# numpy (transitive via azure-ai-projects) ships 3.12+ syntax in its stubs that
# trips mypy pinned to 3.11; we don't call numpy directly.
module = ["numpy", "numpy.*"]
follow_imports = "skip"
follow_imports_for_stubs = true
